var psycholish = angular.module('psycholish', ['ionic','angular-flip','ngTouch','ngAnimate'])    .run(function ($ionicPlatform,$rootScope,$state,wordsService) {        $ionicPlatform.ready(function () {            if (window.cordova && window.cordova.plugins.Keyboard) {                cordova.plugins.Keyboard.hideKeyboardAccessoryBar(true);            }            if (window.StatusBar) {                StatusBar.styleDefault();            }        });        $rootScope.$on('$stateChangeStart',            function(event, toState){                var downloadedAll = wordsService.IsDownloadAll();                if(toState.name!= 'intro'){                    if(!downloadedAll){                        event.preventDefault();                        $state.go('intro');                    }                }                else{                    if(downloadedAll) {                        event.preventDefault();                        $state.go('tabs.letters');                    }                }            });    });psycholish.baseAdress= "http://psycholish.uphero.com/";